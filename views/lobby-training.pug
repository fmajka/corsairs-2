include mixins/lobby-list-el

#lobby-training(x-cloak x-show="$store.router.is($el.id)")
	.game-lobby
		| Trening
		.list.player-list
			+lobby-list-el(true)

		.lobby-settings

	.nav-buttons
		button#ready-btn.bg-darkwood.shade-b(onclick="Corsairs.start(false)" onkeyup="event.preventDefault()") TRENING!
		button.bg-grey.shade-b(@click="$store.router.setView('main-menu')") POWRÓT